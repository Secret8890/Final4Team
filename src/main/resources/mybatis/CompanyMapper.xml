<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gjob.backend.mapper.CompanyMapper">
    <select id="select" resultType="CompanyDTO">
        select * from Company order by co_seq desc limit 30
    </select>
    <select id="selectByEndDate" resultType="CompanyDTO">
        select * from COMPANY where co_end_date not like concat(#{co_end_date},'%') order by co_seq desc limit 30
    </select>
    <select id="selectBySeq" parameterType="String" resultType="CompanyDTO">
        select * from COMPANY where co_seq=#{co_seq}
    </select>
    <insert id="insert" parameterType="CompanyDTO">
        insert ignore into COMPANY(co_seq,co_name,co_name_href,co_title,co_job_name,co_location_name,co_career,co_start_date,co_end_date,co_url,co_job_mid_name) values(#{co_seq},#{co_name},#{co_name_href},#{co_title},#{co_job_name},#{co_location_name},#{co_career},#{co_start_date},#{co_end_date},#{co_url},#{co_job_mid_name})
    </insert>
</mapper>